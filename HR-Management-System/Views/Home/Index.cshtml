@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@{
    ViewData["Title"] = "Dashboard";
    var userRole = ViewBag.UserRole as string;
    var employeeName = ViewBag.EmployeeName as string;
    var pendingRequestsCount = ViewBag.PendingRequestsCount as int? ?? 0;
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-4 border-bottom">
    <h1 class="h2">
        <i class="fas fa-tachometer-alt me-2"></i>Dashboard
    </h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <span class="me-3 text-muted">Welcome, @employeeName</span>
        <div class="btn-group me-2">
            <button type="button" class="btn btn-sm btn-outline-secondary" onclick="exportReport()">
                <i class="fas fa-download me-1"></i>Export
            </button>
        </div>
    </div>
</div>

<!-- Stats Cards -->
<div class="row mb-4">
    @if (userRole == "Admin" || userRole == "HR")
    {
        <div class="col-xl-3 col-md-6 mb-4">
            <div class="card border-left-primary shadow h-100 py-2 stat-card">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">
                                Pending Approvals
                            </div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">@pendingRequestsCount</div>
                        </div>
                        <div class="col-auto">
                            <i class="fas fa-clock fa-2x text-gray-300"></i>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-success shadow h-100 py-2 stat-card">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-success text-uppercase mb-1">
                            Leave Balance
                        </div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800" id="leaveBalance">
                        </div>

                        <div class="h5 mb-0 font-weight-bold" id="leaveBalanceContainer">
                            <span id="leaveBalanceText" class="text-gray-800">
                                <div class="spinner-border spinner-border-sm text-primary" role="status">
                                    <span class="visually-hidden">Loading...</span>
                                </div>
                                Loading...
                            </span>
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-calendar-check fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-info shadow h-100 py-2 stat-card">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                            This Month's Pay
                        </div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800" id="monthlyPay">
                            <div class="spinner-border spinner-border-sm text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                            Loading...
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-dollar-sign fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-xl-3 col-md-6 mb-4">
        <div class="card border-left-warning shadow h-100 py-2 stat-card">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                            My Requests
                        </div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800" id="myRequests">
                            <div class="spinner-border spinner-border-sm text-primary" role="status">
                                <span class="visually-hidden">Loading...</span>
                            </div>
                            Loading...
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-list fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Quick Actions -->
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header bg-white">
                <h5 class="card-title mb-0">
                    <i class="fas fa-bolt me-2"></i>Quick Actions
                </h5>
            </div>
            <div class="card-body">
                <div class="row text-center">
                    <div class="col-md-3 mb-3">
                        <a href="/Leave/Apply" class="btn btn-primary btn-lg w-100 py-3">
                            <i class="fas fa-plus-circle fa-2x mb-2"></i><br>
                            Apply for Leave
                        </a>
                    </div>
                    <div class="col-md-3 mb-3">
                        <a href="/Leave/MyRequests" class="btn btn-success btn-lg w-100 py-3">
                            <i class="fas fa-history fa-2x mb-2"></i><br>
                            My Leave History
                        </a>
                    </div>
                    <div class="col-md-3 mb-3">
                        <a href="/Payroll/MyPayslips" class="btn btn-info btn-lg w-100 py-3">
                            <i class="fas fa-file-invoice-dollar fa-2x mb-2"></i><br>
                            My Payslips
                        </a>
                    </div>
                    <div class="col-md-3 mb-3">
                        <a class="btn btn-warning btn-lg w-100 py-3" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
                            <i class="fas fa-user-edit fa-2x mb-2"></i><br>
                            My Profile
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Activity -->
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header bg-white d-flex justify-content-between align-items-center">
                <h5 class="card-title mb-0">
                    <i class="fas fa-history me-2"></i>Recent Activity
                </h5>
                <a href="/Leave/MyRequests" class="btn btn-sm btn-outline-primary">View All</a>
            </div>
            <div class="card-body">
                <div id="recentActivity">
                    <div class="text-center py-4">
                        <div class="spinner-border text-primary" role="status">
                            <span class="visually-hidden">Loading...</span>
                        </div>
                        <p class="mt-2">Loading recent activity...</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Load dashboard data via AJAX
            loadDashboardData();
        });

        function loadDashboardData() {
            $.ajax({
                url: '/Home/GetDashboardData',
                type: 'GET',
                success: function(data) {
                          if (data.success) {
            var balance = data.leaveBalance;
            var balanceText = balance + ' days';
            var balanceElement = $('#leaveBalanceText');

            // Remove existing colors
            balanceElement.removeClass('text-success text-warning text-danger text-gray-800').addClass('text-gray-800');

            // Apply new color based on logic (e.g., Warning if < 10, Danger if < 5)
            if (balance <= 0) {
                balanceElement.addClass('text-danger');
            } else if (balance < 5) {
                balanceElement.addClass('text-danger');
            } else if (balance < 10) {
                balanceElement.addClass('text-warning');
            } else {
                balanceElement.addClass('text-success');
            }

            balanceElement.html(balanceText);

            // Update other stats
            $('#monthlyPay').html('$' + data.monthlyPay.toLocaleString('en-US', {minimumFractionDigits: 2, maximumFractionDigits: 2}));
            $('#myRequests').html(data.myRequests);
            $('#recentActivity').html(data.recentActivityHtml);
        } else {
                        showDefaultData();
                    }
                },
                error: function(xhr, status, error) {
                    console.error('Error loading dashboard data:', error);
                    showDefaultData();
                }
            });
        }

        function showDefaultData() {
            $('#leaveBalance').html('21 days');
            $('#monthlyPay').html('$3,000.00');
            $('#myRequests').html('0');
            $('#recentActivity').html('<p class="text-muted">No recent activity</p>');
        }

        function exportReport() {
            alert('Export functionality would be implemented here');
        }
    </script>
}